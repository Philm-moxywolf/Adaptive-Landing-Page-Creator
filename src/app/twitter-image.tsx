// Reuse the dynamic branded OG card for the Twitter card too, so the Twitter
// surface always has an image even when no bespoke card is configured.
export { default, size, contentType, alt } from "./opengraph-image";
